services:
  user-service:
    build: 
      context: ./user-service
      dockerfile: Dockerfile
    container_name: user-service-app
    ports:
      - "3001:3000"
    restart: always

  data-siswa-service:
    build: 
      context: ./data-siswa-service
      dockerfile: Dockerfile
    container_name: data-siswa-service-app
    ports:
      - "3002:3000"
    restart: always
